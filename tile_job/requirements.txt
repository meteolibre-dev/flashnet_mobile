# Tile Job Dependencies
# Minimal dependencies for generating PNG tiles from COG files
# Run on a low-cost CPU machine (no GPU needed)

# GCS
google-cloud-storage>=2.14.0
google-auth>=2.27.0

# Raster processing
rasterio>=1.3.0
rio-tiler>=6.0.0

# Data processing
numpy>=1.26.0
Pillow>=10.0.0
matplotlib>=3.8.0

# Logging
python-dotenv>=1.0.0